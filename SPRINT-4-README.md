# Sprint 4 — Railway Deployment

החבילה מוסיפה תצורת פריסה ל-Railway עבור אתר HTML/CSS/JavaScript סטטי.

## קבצים חדשים

```text
Dockerfile
Caddyfile
.dockerignore
railway.toml
docs/RAILWAY_DEPLOYMENT.md
```

## התקנה

חלצו את קובץ ה-ZIP לתוך שורש המאגר `datascope-course`.

לאחר המיזוג, המבנה צריך להתחיל כך:

```text
datascope-course/
├── Dockerfile
├── Caddyfile
├── railway.toml
├── .dockerignore
├── website/
└── docs/
```

## Commit מוצע

```bash
git add .
git commit -m "Add Railway deployment configuration"
git push origin main
```

לאחר ה-push אפשר לחבר את המאגר ל-Railway.
